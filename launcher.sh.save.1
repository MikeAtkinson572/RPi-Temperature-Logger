#!/bin/sh
# launcher.sh

# Temperature logger disabled from auto starting
# by disabling in crontab. invoke by using  sudo crontab -e

cd /
cd home/pi/TemperatureLogger
sudo /usr/bin/python3.2 templog.py
cd /
